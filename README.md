# YNot&S

App simples de notas com login, feito em Python (WSGI) e Firebase Realtime Database.

## Configuração
Define a variável de ambiente `FIREBASE_URL` com o URL da tua Realtime Database.

## Deploy
Pronto para Render/Railway/Heroku (usa o Procfile incluído).
